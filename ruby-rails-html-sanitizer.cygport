inherit rubygem

NAME="ruby-rails-html-sanitizer"
VERSION=1.0.3
RELEASE=1
CATEGORY="Ruby"
SUMMARY="Rails HTML sanitizer API"
DESCRIPTION="HTML sanitization for Rails applications"

ARCH=noarch

PKG_NAMES="${NAME} ${NAME}-doc"
ruby_rails_html_sanitizer_CONTENTS="${RUBYGEM_INSTDIR#/} ${RUBYGEM_SPEC#/}"
ruby_rails_html_sanitizer_doc_CONTENTS=${RUBYGEM_DOCDIR#/}
